# 코드 작업 지침

적용: 종로5가 투자지도 개발기준 v1.2, 2026-09-22.

## 시작

현재 작업 항목에 속하지 않는 파일과 동작은 변경하지 않는다. 수정이 필요하면 별도 작업으로 분리하고 이유를 기록한다.

실제 저장소와 기존 지침을 먼저 읽는다. 저장소명·기본 브랜치·공개 여부·기존 파일을 추정하지 않는다. 현재 릴리스와 해당 인수조건은 `docs/RELEASE_PLAN.md`, 설계 결정은 `docs/ADR.md`, 데이터 계약은 `docs/DATA_DICTIONARY.md`를 따른다. `archive/`는 구현 요구사항이 아니다.

## 구성

정적 HTML·CSS·JavaScript ES Modules, 고정 버전 MapLibre, 스마트폰 IndexedDB, 개인 PC의 SQLite 정본, Python CLI를 사용한다. 앱 실행에 Node.js·번들러를 요구하지 않는다. 지도 실패 시 목록·기록·내보내기를 제공한다. 지도 자료 이용조건은 렌더러와 별도로 확인한다.

## 첫 구현 범위

R1a는 현장 신규 관측·사진·단일 ZIP 내보내기다. 정본 갱신·가격 변경·거래 연결을 폰에서 편집하지 않는다. R1b는 단일 PC의 반영·중복 방지·백업·복구다. 반영확인서, 변경 병합, 벡터시계, 복수 기기 동시 편집은 만들지 않는다. 실제 사용에서 필요가 확인되면 새 ADR로 검토한다.

## 기록 보존

관측일·기기 입력일·정본 반영일을 구분한다. 내보낸 이벤트는 고정하며 정정은 새 ID로 추가한다. 동일 이벤트 ID·동일 해시는 재반영하지 않고, 동일 ID·상이 해시는 거절한다. 미반영·미확인 기록을 자동 삭제하지 않는다. 휴대폰의 내보내기 성공을 PC 반영·백업 완료로 표시하지 않는다.

## 데이터

PNU는 필지 외부 ID, `asset_id`는 매입 검토 단위다. 여러 필지·건물 구성을 허용한다. 결측은 `null`과 사유다. 실거래·호가·현장 관측·전달정보·추정·가정을 분리한다. 핵심 값에 근거와 시점을 연결한다. 마스킹된 거래를 추정으로 특정 PNU에 확정하지 않는다.

FAR 산정용 연면적과 총연면적을 분리한다. 대지 산정과 건물 배치 제약을 혼용하지 않는다. 면적·규제 미확인 시 용적률 여유면적 계산을 중단한다. 보증금 차감 전 계약 총액을 사용하고 승계 반환 의무를 보존한다. 미확인 세무·금융 항목을 0으로 처리하지 않는다.

## 공간·조사

점포 ID는 물리적 조사 단위다. 업체명 변경으로 새 점포 ID를 만들지 않는다. 경로 버전·표본틀·실제 조사 세션을 구분한다. 미조사 점포를 정상 영업으로 채우지 않는다. 비교 지역은 거래 우선이며 상세 필지·대장은 지정 대상만 수집한다.

## 정본·파생본

모든 DB 연결에서 외래키를 활성화한다. 정본 반영은 트랜잭션으로 처리한다. 파생본은 전체 재생성 후 검증해 게시하며 일부만 갱신하지 않는다. 실패 시 이전본과 실제 버전을 유지한다. 오래된 파생본을 최신 데이터라고 표시하지 않는다. 백업은 일관된 DB 사본과 참조 파일로 만들고 빈 폴더 복구를 시험한다.

## 보안

실데이터 DB·사진·개인 메모·권리관계 자료·API 키를 Git과 공개 배포에 넣지 않는다. 소유자 이름·주소·연락처를 저장하지 않는다. 공개 앱에는 코드·가상자료만 둔다. 임의 HTML 실행, ZIP 경로 탈출·과도한 압축 해제, 로그의 비밀키를 차단한다. 기기 데이터의 외부 전송·유료 서비스·배포·실데이터 업로드는 사용자 승인 후 수행한다.

## 수집·완료

공식 API·허용한 파일만 수집한다. 약관·승인·실제 응답을 먼저 확인한다. 웹사이트 차단 우회나 비공개 API 추측으로 수집하지 않는다. API 실패·빈 결과·일부 페이지 누락을 구분한다. 작업마다 테스트·실측 시간·남은 제한을 기록한다. 가상자료 테스트 통과를 실제 API·기기·법률 검증 완료로 설명하지 않는다.
